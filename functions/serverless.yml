service: aws-container-lambda
framework Version: "3"
provider:
  name: aws
  lambdaHashingVersion: 20201221
  ecr:
    images:
      aws-lambda-test:
        path: ./
functions:
  hello:
    image:
      name: aws-lambda-test
      command:
        - app.handler